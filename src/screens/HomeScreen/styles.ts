import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    fontSize: 30,
    borderWidth: 1,
    borderColor: 'red',
  },
});

export default styles;
